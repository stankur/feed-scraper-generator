[project]
name = "oss-feed-scraper-generator"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Stanley Kurniawan", email = "terserahskurniawan@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "browser-use>=0.9.7",
    "python-dotenv>=1.2.1",
]

[build-system]
requires = ["uv_build>=0.8.15,<0.9.0"]
build-backend = "uv_build"
